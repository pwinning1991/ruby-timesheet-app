<div class="logo">
	<h1>Time Tracker </h1>

</div>
<% if current_user %>
  <ul class="nav nav-tabs custom-nav">
    <li class="nav-item <%= active?(root_path) %>">
      <%= link_to "Home", root_path %>
    </li>
    <li class="nav-item <%= active?(posts_path) %>">
      <%= link_to "Time Entries", posts_path %>
    </li>
    <%= render 'shared/audit_log_tab' if policy(AuditLog).index? %>
    <li class="nav-item <%= active?(new_post_path) %>">
      <%= link_to "Add New Time Entry", new_post_path, id: 'new_post_from_nav' %>
    </li>
    <li class="nav-item dropdown pull-right">
       <a class="nav-link dropdown-toggle" href="http://example.com" id="navbarDropdownMenuLink" data-toggle="dropdown">Options</a>
      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
          <div class="dropdown-divider">
          <% if current_user.try(:type) == 'AdminUser' %>
          <%= link_to "Admin Dashboard", admin_root_path %>
          <% end %>
        </div> 
            <%= link_to "Logout", destroy_user_session_path , method: :delete %>
      </div>
    </li>
  </ul>
<% end %>
